# ============================================================
# GloryPrep — Environment Variable Template
# ------------------------------------------------------------
# Use this as the shape for your local .env (never commit it)
# and for Vercel environment variables
# (Project > Settings > Environment Variables).
# ============================================================

DATABASE_URL=postgresql://user:password@host/gloryprep?sslmode=require
JWT_SECRET=<generate: openssl rand -hex 32>
SITE_URL=https://gloryprep.com

# PayPal (Live account — from developer.paypal.com → My Apps & Credentials)
PAYPAL_CLIENT_ID=your_paypal_live_client_id
PAYPAL_SECRET=your_paypal_live_secret
PAYPAL_BASE_URL=https://api-m.paypal.com

# PayPal webhook (created at developer.paypal.com → Webhooks → Add webhook)
# URL: https://your-domain.com/api/pay/webhook  Events: CHECKOUT.ORDER.APPROVED, PAYMENT.CAPTURE.COMPLETED, PAYMENT.CAPTURE.DENIED, PAYMENT.CAPTURE.REFUNDED, PAYMENT.CAPTURE.REVERSED
PAYPAL_WEBHOOK_ID=your_paypal_webhook_id

# Optional analytics (Umami-compatible)
VITE_ANALYTICS_WEBSITE_ID=
VITE_ANALYTICS_ENDPOINT=https://
